# VadCLIP-UCF exp19 (TCN head) — Python 3.8
# Tested on macOS (CPU) with conda env `vadclip`.
# For CUDA: match torch/torchvision to your CUDA version (see pytorch.org).

# Core ML
torch==2.4.1
torchvision==0.20.0
numpy>=1.24,<2.0
scipy==1.10.1
scikit-learn==1.3.2
pandas==2.0.3

# CLIP (OpenAI ViT-B/16) dependencies
ftfy==6.2.3
regex==2024.11.6
Pillow>=9.0
setuptools  # for pkg_resources (used by src/clip/clip.py)

# Utilities
tqdm==4.67.1

# Dev / tests
pytest==8.3.5
